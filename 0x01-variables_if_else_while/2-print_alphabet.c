#include <stdio.h>
/**
 * main - the main function
 *
 * Return: the main function returns 0
 */

int main(void)
{
	char i;

	for (i = 'a' ; i <= 'z' ; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
