#include <stdio.h>

/**
 * main - the function in which most functions run
 *
 * Return: at the end of main retrun 0
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
