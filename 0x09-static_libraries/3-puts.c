#include "main.h"
#include <stdio.h>

/**
 * _puts - output funtion
 * @s: parameter
 */

void _puts(char *s)
{
	puts(s);
}
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}

